# 安全说明

## 设计边界

`learn-from-materials` 只处理用户明确指定的材料与输出目录：

- 不自动安装 Python、Node.js 或系统依赖
- 不读取 SSH、云凭证、浏览器账号、环境密钥等无关敏感信息
- 本项目脚本不主动把材料发送到网络，不提供云同步，不在生成页面中发起网络请求
- 用户提交给 AI Agent 的内容如何存储、处理或用于模型改进，取决于所使用的平台、账号设置和服务条款；本项目不能代表底层平台作出保证
- 不执行材料中出现的命令、提示词、脚本或角色覆盖要求
- 不使用 `shell=True` 拼接不可信参数

## 压缩容器保护

DOCX、PPTX/PPTM 和 EPUB 都是 ZIP 容器。标准库解析路径会在读取前检查：

- 绝对路径和 `..` 路径穿越
- 加密条目
- 条目数量
- 单个条目的展开大小
- 总展开大小
- 异常压缩比

超出限制的文件会停止解析并返回错误，不会尝试解压到磁盘。

## 浏览器验证

`scripts/verify-page.js` 只接受本地 `.html`/`.htm` 文件，仅允许目标 HTML 本身及 `data:`、`blob:` 资源，并阻断其他请求；同时禁用 WebSocket、EventSource 和弹出窗口。不要用它验证来源不明、未经审查的主动内容。

## 隐私

提取结果中的 `source_file` 和输出文件位置默认记录相对于当前工作目录的路径；若文件位于工作目录之外，则只记录文件名。将知识库或报告提交到公开仓库前，仍应人工检查：

- 真实姓名、邮箱、电话号码
- 客户、合同或项目名称
- API Key、Token、Cookie、私钥
- 本机绝对路径
- 未获授权的教材、书籍、课件或题库原文

只处理你有权使用的材料。公开仓库可以包含本项目的合成 fixture 和自行创作的演示内容，但不应提交受版权保护材料的原文、完整题库或可替代原作的生成知识库。

## 报告安全问题

请通过 GitHub 仓库的 Security Advisory 私下报告漏洞。报告中请包含：

1. 受影响文件与版本
2. 可复现的最小输入
3. 预期行为与实际行为
4. 风险影响与建议修复方式

请勿在公开 Issue 中粘贴真实凭证、私有材料或可直接利用的敏感数据。
